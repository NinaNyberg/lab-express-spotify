<ul>
  {{#each albums}}
    <li>
      {{#if images.length}}
        <img src='{{images.0.url}}' alt='{{name}}' />
      {{/if}}
      <h4>{{name}}</h4>
      <a href='/track-list/{{id}}'>View Tracks</a>
    </li>
  {{/each}}
</ul>